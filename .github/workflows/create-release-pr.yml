name: Legacy create release PR disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: GitHub Flow uses main PRs
    runs-on: ubuntu-latest

    steps:
      - name: Explain disabled workflow
        run: |
          echo "This repository now uses GitHub Flow."
          echo "Create release PR workflow is disabled because release/* branches are no longer used."
          exit 1
